import React from 'react';

// Internal imports
import Login from './Login';
import ForgotPassword from './ForgotPassword';
import ResetPassword from './ResetPassword';

/**
 * Exports the Login page component for use in routing.
 * @component
 * @type {React.FC}
 */
export { Login };

/**
 * Exports the ForgotPassword page component for use in routing.
 * @component
 * @type {React.FC}
 */
export { ForgotPassword };

/**
 * Exports the ResetPassword page component for use in routing.
 * @component
 * @type {React.FC}
 */
export { ResetPassword };